#include <stdio.h>
#include "main.h"
#include <stdlib.h>
void print_binary(unsigned long int n)
{
	char *buffer = malloc(sizeof(char) * 64);
	char *p = buffer;

	do
	{
		*p++ = (n % 2) ? '1' : '0';
		n /= 2;
	}while (n > 0);
	while (p-- > buffer)
	{
		putchar(*p);
	}
}
